# Dependencies
node_modules/

# Build output — the exes are ~90 MB each, well past what belongs in git.
# Ship them as GitHub Release attachments instead.
dist/

# Generated from build/source-icon.png by `npm run icon`
build/icon.ico
build/icon.png

# Local library when running from source
.utune-data/

# Editor / OS noise
.vscode/
.idea/
Thumbs.db
desktop.ini
*.log

# Ambience source recordings. The shipped seamless loops in resources/ambience
# are built from these by build/make-all-loops.js; the sources are 41 MB and do
# not need to be in the repo.
resources/ambience-src/
